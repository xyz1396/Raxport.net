# AGENTS.md

Repo-specific instructions for Codex and other coding agents.

## env

- `python` and `rg` is available in the micromamba environment named `sipros5`.
- `.NET 8 SDK` is available in the micromamba environment named `sipros5` via `dotnet-sdk`.
- Run them via `micromamba run -n sipros5 ...` when using them from Codex.
